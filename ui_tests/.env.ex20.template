# Example environment variables for EX20 Externship
# Copy this file to `.env.ex20` and fill in secret values. Do NOT commit your local `.env.ex20`.

# Externship Identifier
EXTERNSHIP=EX20

# User Credentials for EX20
STUDENT_EMAIL=
STUDENT_PASSWORD=
MENTOR_EMAIL=
MENTOR_PASSWORD=
ADMIN_EMAIL=
ADMIN_PASSWORD=
BASE_URL=https://stage.tripleten.percruit.com/

# UI / Playwright
UI_BASE_URL=http://localhost:3000
UI_USER_EMAIL=example@example.com
UI_USER_PASSWORD=changeme
# Set to 'false' to run browser in headed mode (visible browser window)
PLAYWRIGHT_HEADLESS=true
# Delay in milliseconds between browser actions (useful for debugging)
PLAYWRIGHT_SLOWMO=100
# Wait time in milliseconds before closing browser/page (useful for visual inspection)
TEARDOWN_DELAY=8000

# API / Pytest
API_BASE_URL=http://localhost:8000
API_USER_EMAIL=example@example.com
API_USER_PASSWORD=changeme
API_TOKEN=
