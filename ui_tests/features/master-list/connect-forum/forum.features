
Feature: Percuit Website Student-Connect-Forums test

Scenario: When I add the title "When user goes to the Forum page, the page loads as expected" to the log 
    Given the Student is authenticated in the system
    When the user navigates to the Forum page
    Then the Forum page displays

Scenario: When I add the title "Student cancels post creation" to the log 
  Given the Student is on the Forum page
  When the student clicks the "New Post" button
  Then the new post modal should appear
  When the student clicks the "Cancel" button in the modal
  Then the new post modal should close
  And the student should remain on the "Forum" page

 Scenario: When I add the title "Student tries to create a post without entering a title" to the log
  Given the student is on the Forum page
  When the student clicks the "New Post" button
  Then the new post modal should appear
  When the student leaves the title field empty
  And enters valid content
  And clicks "Create Topic"
  Then an error message should appear saying "Title is required"
  And the post should not be published
